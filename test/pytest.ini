[pytest]
filterwarnings =
    ignore:UserWarning
addopts =
    --ignore=test/test_api_manual.py --ignore test_api_manual.py
markers =
    auth: authentication tests
    cart: cart tests
    catalog: catalog tests
    spamcart: spams the cart for receiving requests
